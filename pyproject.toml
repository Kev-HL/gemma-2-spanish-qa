[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gemma-2-qa-spanish"
version = "0.1.0"

[tool.pytest.ini_options]
# Markers for filtering tests
markers = [
    "integration: marks tests as integration (require external resources)",
]